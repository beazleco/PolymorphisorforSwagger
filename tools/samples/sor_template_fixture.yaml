openapi: 3.0.3
info:
  title: Vertexon CMS, synthetic SOR specification
  version: 0.0.1-fixture
  description: "TEST FIXTURE, NOT SOR DOCUMENTATION. Generated by tools/build_sor_fixture.py from the
    SOR column values of the mapping workbook, so most of them resolve against it by construction. Three
    defects are deliberate: 5 leaf names are omitted, 'currencyCode' is duplicated under 'auditTrail',
    and /v1/card/cancel declares only PUT. Replace this with the real SOR specification before drawing
    any conclusion from a run."
  x-fixture: true
  x-omitted-leaves:
  - accountName
  - cancelDate
  - customerLimitCurrency
  - issueReason
  - postingTime
paths:
  /v1/account/contact:
    get:
      operationId: sorV1AccountContact
      summary: SOR operation /v1/account/contact
      responses:
        '200':
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    type: string
                  phoneNo:
                    type: string
                  phoneType:
                    type: string
      parameters:
      - name: account
        in: query
        required: false
        schema:
          type: string
      - name: currencyCode
        in: query
        required: false
        schema:
          type: string
      - name: includePhone
        in: query
        required: false
        schema:
          type: string
  /v1/account/info:
    get:
      operationId: sorV1AccountInfo
      summary: SOR operation /v1/account/info
      responses:
        '200':
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    type: string
      parameters:
      - name: account
        in: query
        required: false
        schema:
          type: string
      - name: currencyCode
        in: query
        required: false
        schema:
          type: string
  /v1/card/account/list:
    get:
      operationId: sorV1CardAccountList
      summary: SOR operation /v1/card/account/list
      responses:
        '200':
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  accountNumber:
                    type: string
                  cards:
                    type: object
                    properties:
                      cardToken:
                        type: string
                      expiryDate:
                        type: string
      parameters:
      - name: account
        in: query
        required: false
        schema:
          type: string
      - name: currencyCode
        in: query
        required: false
        schema:
          type: string
      - name: includeCards
        in: query
        required: false
        schema:
          type: string
  /v1/deposit/account/balance:
    get:
      operationId: sorV1DepositAccountBalance
      summary: SOR operation /v1/deposit/account/balance
      responses:
        '200':
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  asOf:
                    type: string
                  balAmt:
                    type: string
                  balCcy:
                    type: string
                  balType:
                    type: string
      parameters:
      - name: accountNo
        in: query
        required: false
        schema:
          type: string
      - name: ccy
        in: query
        required: false
        schema:
          type: string
  /v1/replace/me:
    get:
      operationId: sorV1ReplaceMe
      summary: SOR operation /v1/replace/me
      responses:
        '200':
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sorFieldName:
                    type: string
      parameters:
      - name: sorFieldName
        in: query
        required: false
        schema:
          type: string
